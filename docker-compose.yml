version: '2'
services:
    relay:
        build: .
        volumes:
          - ./data:/data
        environment:
          - BTCD_TESTNET=1
        env_file:
          - variables.env
        ports:
          - 0.0.0.0:30303:30303
        logging:
          driver: "json-file"
          options:
            max-size: "1m"
            max-file: "10"
